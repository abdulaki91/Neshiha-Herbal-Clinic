# Used automatically by `npm run build` (Vite's default mode is
# "production", which loads this file over .env for matching keys) —
# no manual configuration needed on whatever platform builds this.
VITE_API_URL=https://nesihaherbalclinic-backend.abdulaki.com/api/v1
VITE_SOCKET_URL=https://nesihaherbalclinic-backend.abdulaki.com
